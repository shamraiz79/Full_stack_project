# ShopEase Backend

A minimal Express backend for the ShopEase project.

## Setup

npm install

## Run

npm run dev

## Endpoints

- GET /
- GET /api/health

## Environment

Create a .env file with:

PORT=5000
MONGODB_URI=mongodb://localhost:27017/shopease
JWT_SECRET=your_jwt_secret_here
